//! The Observation Engine: diffs provider snapshots into domain state
//! (`NetworkConnection` lifecycle, correlation). The only component
//! permitted to construct Engine-owned types. See `docs/ARCHITECTURE.md`.
//! Populated starting Phase 0.1.
