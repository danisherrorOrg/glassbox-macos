//! Shared test fixtures and mock provider implementations (fixture
//! `SocketObservation`/`ProcessObservation` values) used by unit tests
//! across `models`/`providers`/`engine`. See `docs/TESTING_STRATEGY.md`.
//! Populated starting Phase 0.1.
