//! Tauri `invoke` command handlers exposed to the frontend
//! (`get_processes`, `get_connections`, ...). See `docs/OBSERVATION_CONTRACT.md`
//! for the response envelope shape. Populated starting Phase 0.1.
