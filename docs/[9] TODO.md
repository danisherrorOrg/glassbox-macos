# TODO / Roadmap — Process Network Inspector

**v5 — migrated to the Tauri (Rust) + React stack (ADR-013).** Earlier versions of this file described FastAPI/uvicorn tasks, and before that Xcode/SwiftUI/Swift-concurrency tasks, that no longer apply — see `docs/DECISIONS.md` for why the stack changed each time. Ordered so each phase produces something demoable before the next one starts — don't jump ahead to a later phase's checkboxes while earlier ones are unchecked.

Two things stay true across every phase below, not just the ones that mention them: **redaction of highly-sensitive fields happens at capture time and never produces a raw value anywhere; potentially-sensitive fields are redacted before anything touches disk, not just before it touches the screen** (`docs/PRIVACY_AND_SECURITY.md`), and **unknown/denied/stale is never displayed as empty/zero** — see the Observation Contract tasks below.

---

**Before Phase 0:** complete `pre-implementation/[3] TODO.md` — a design-verification
gate run against this document set, separate from and prior to the checklist below.

## Phase 0 — Foundation (project setup, not a product milestone)

- [x] Set up the Tauri project: `cargo tauri init`, a `Cargo.toml` for the Rust core, `tauri.conf.json` configured with a minimal command allowlist/capabilities set (not "allow everything"). Done 2026-09-07: scaffolded via `npm create tauri-app` (Tauri v2, React-TS template) rather than the bare `cargo tauri init` — same result, generates both halves pre-wired for `invoke`/`event` instead of two separate manual steps. Verified with `cargo check` (clean) and a real `cargo tauri dev` run (compiled, launched, stayed up, no panics) before removing the scaffold's demo `greet` command.
- [x] Set up the frontend project: React (via Vite, Tauri's default template integration), talking to the Rust core only via `invoke`/`event` — no REST/WebSocket endpoints to stand up. Done: same scaffold command as above; `npx tsc --noEmit` clean.
- [x] `git init`, initial commit, `.gitignore` for Rust + Node (`target/`, `node_modules/`, `dist/`). `git init`/`.gitignore` predate this phase (from the pre-implementation work). Note: the scaffolding tool's `-f` force flag overwrote `.gitignore` and briefly deleted `docs/`/`pre-implementation/` from the working tree — caught immediately and restored via `git restore` (both were already committed, so nothing was lost) before the initial project commit landed.
- [x] Set up folder structure: `src-tauri/` (`src/` with `models/`, `providers/`, `engine/`, `commands/`, and `tests/`), `src/` for the React frontend (`components/`, `api/` for the `invoke` wrapper functions). Done: each Rust module is a real (currently empty, doc-comment-only) module wired into `lib.rs`, `tests/` gated behind `#[cfg(test)]`; `src/components/`, `src/api/` are placeholder dirs (`.gitkeep`) — no code yet, that's Phase 0.1.
- [x] **Permissions spike (do this before writing any provider code):** run the five-question checklist in `docs/PERMISSIONS_AND_PLATFORM.md`'s "First technical spike" section — privilege (same-user and other-users' visibility) *and* the three data-shape questions (PID attribution, byte counters, socket state) that `SocketObservation`'s fields actually depend on. Write findings into that document's VERIFIED section — this determines how much of Phase 0.1 is trivial vs. blocked, and a privilege-only check is not sufficient to answer that. Already done as part of `pre-implementation/[3] TODO.md` step F, 2026-09-07 — see `docs/PERMISSIONS_AND_PLATFORM.md`'s "First technical spike" section for full results (all five questions answered; byte counters confirmed unavailable, everything else confirmed positive).
- [x] Decide the Tauri capabilities/allowlist scope now, not later: only the specific commands the frontend needs, nothing broader (`docs/PRIVACY_AND_SECURITY.md`) — this is a default to get right from the first commit, not a hardening pass to do at the end. (There is no server binding/CORS decision to make — see `docs/ARCHITECTURE.md`'s "No local network surface.") Done: `src-tauri/capabilities/default.json` scoped to `core:default` only — dropped the scaffold's `opener:default` (unused plugin, no external-link-opening in this app) and the demo `greet` command/handler. No product commands exist yet (Phase 0.1 adds `get_processes`/`get_connections`); the allowlist starts at zero product surface, not "allow everything and narrow later."

## Phase 0.1 — Process Explorer + Socket Explorer (first product milestone)

*Goal: process list → click a process → see its live socket table. First demo.*

**Models & providers**

- [x] Define `ProcessObservation` (provider-owned, raw), `ProcessSnapshot` (provider-owned, adds `ProviderStatus`), and `ProcessInfo` (Engine-owned, adds `process_state` and `status: ObservationStatus`) per `docs/DATA_MODEL.md` — a provider must never construct `ProcessInfo` directly, same discipline as the socket split below. Done 2026-09-07: `src-tauri/src/models/process.rs`.
- [x] Define `SocketObservation` (provider-owned, raw — no identity or lifecycle fields) and `SocketSnapshot` (immutable, point-in-time list of `SocketObservation`s, plus a `ProviderStatus`) per `docs/DATA_MODEL.md`. Done: `src-tauri/src/models/socket.rs`.
- [x] Define `NetworkConnection` (Engine-owned domain state — `connection_id`, `lifecycle_state`, `first_seen`, `last_seen`, `status`) — a provider must never construct this type directly. Done: `src-tauri/src/models/connection.rs`.
- [x] Define `ProviderStatus` (state, observed_at, reason?) and `ObservationStatus` (state, observed_at, last_successful_at?, reason?, provider?) per `docs/DATA_MODEL.md` / `docs/OBSERVATION_CONTRACT.md` — a provider constructs `ProviderStatus`; only the Engine constructs `ObservationStatus`. Done: `src-tauri/src/models/status.rs`, including `ObservationStatus::from_provider` — the one function implementing the staleness-promotion rule (`OBSERVATION_CONTRACT.md`), covered by the 4th mandatory Engine test.
- [x] Define the `Flow` struct per `docs/DATA_MODEL.md` — defined now, not wired into the Engine or UI until Phase 0.5 (`docs/DECISIONS.md` ADR-004). Done: `src-tauri/src/models/flow.rs` — kept to `flow_id`/`connection_id` only, since `ResolvedHostname`/`HTTPRequest`/`HTTPResponse` (its other fields per `DATA_MODEL.md`) don't exist as types yet.
- [x] Define `ProcessProvider` as a Rust trait returning a `ProcessSnapshot`. Done: `src-tauri/src/providers/process.rs`.
- [x] Implement `ProcessProvider` (`sysinfo` crate, falling back to direct `libproc` FFI if `sysinfo` doesn't cover a needed field). Done: `SysinfoProcessProvider`, with a `proc_pidpath` FFI fallback for `executable_path` when `sysinfo::Process::exe()` comes back empty — matches the Phase 0 spike's finding that `proc_pidpath` is unprivileged-accessible even cross-user.
- [x] Define `SocketProvider` trait (returns a `SocketSnapshot` of `SocketObservation`s — never a `NetworkConnection`). Done: `src-tauri/src/providers/socket.rs` — also adds `is_permitted(pid)`, needed because `netstat2` can't itself signal `permission_denied` (`PIF-046`).
- [x] Implement `SocketProvider` (`netstat2`/`sysinfo` where available, direct `libproc` FFI fallback — see the Phase 0 permissions spike's findings on which is actually required). Done: `NetstatSocketProvider` — no `libproc`-FFI-from-day-one fallback needed, confirmed sufficient by the spike. `is_permitted` goes through direct `libproc` (`providers/system.rs::owner_uid`, `PROC_PIDTBSDINFO`), not `sysinfo::Process::user_id()` (found unreliable by the spike).
- [x] Stub bare trait definitions for `DNSProvider` and `TrafficProvider` — signatures only, minimal. Don't design their final shape now: the Phase 0.3 mitmproxy spike will reveal real constraints a premature interface would likely get wrong, and ADR-003's guardrail (no swappable-backend machinery before a second implementation exists) applies to interface *detail*, not just to whether an interface exists at all. Done: `src-tauri/src/providers/{dns,traffic}.rs` — genuinely empty marker traits.
- [x] Implement provider-level statuses via `ProviderStatus`: `observed` / `unavailable` / `permission_denied` / `unsupported` / `transient_failure` per `docs/OBSERVATION_CONTRACT.md` — providers never emit `stale` or `unmatched`, those are Engine-derived, and a provider never constructs `ObservationStatus` directly. Done — `ProcessProvider`/`SocketProvider` both return `observed`/`transient_failure` (the two states their real failure modes can actually produce); `permission_denied` is produced by the Engine via `is_permitted`, not by the provider's own `ProviderStatus`, per the spike's guidance.

**Observation Engine**

- [x] Define `ObservationEngine` responsibilities explicitly: consume `SocketSnapshot`s, diff successive snapshots into `NetworkConnection` lifecycle events, merge in `ProcessProvider` output, maintain current process/connection state, wrap each provider's `ProviderStatus` into the `ObservationStatus` it attaches to everything it emits, and emit normalized updates via Tauri commands/events — providers never talk to Tauri or React directly. Done: `src-tauri/src/engine/mod.rs`.
- [x] Implement connection identity as an Engine session identity, not an OS-level one, per the explicit matching rule in `docs/DATA_MODEL.md` (`NetworkConnection`'s "Connection-identity matching rule" subsection): exact-tuple match against the immediately preceding *successful* snapshot, no numeric confidence score in this phase. When a match is ambiguous (matches more than one tracked connection equally well), **prefer creating a new connection over merging into an existing one** — a false split is cosmetic, a false merge corrupts the timeline. Done: `refresh_connections`'s `tuple_to_id`/`seen_ids` matching.
- [x] Implement the `closed` vs. `expired` rule explicitly (`docs/DATA_MODEL.md`): `closed` requires positive evidence the connection ended — in this phase, that's exclusively confirmed process exit (`ProcessProvider` reporting the PID gone); a connection that simply stops appearing in snapshots becomes `expired`, never silently `closed`. Expect `discovered → active → expired` to be the normal path this phase, `→ closed` only via the process-exit path. A `SocketSnapshot` whose own `status` isn't `observed` (`transient_failure`/`unavailable`/`permission_denied`) is not evidence of anything — leave every tracked connection's `lifecycle_state`/`last_seen` untouched on such a poll and update only `status`. Done, covered by `process_termination_closes_its_connections`/`polling_gap_expires_not_closes`/`provider_failure_does_not_manufacture_state`.
- [x] Implement the `discovered` vs. `active` transition per `docs/DATA_MODEL.md`: `discovered` on first sight, `active` on a second consecutive successful match or an observed `ESTABLISHED` state (including `LISTEN` sockets, which follow the same rule). Done, covered by `discovered_promotes_to_active_on_second_match`.
- [x] Test connection matching against reused local ports and rapidly closed/reopened connections. Done: `reused_local_port_after_gap_is_a_new_connection_id` — confirms a tuple reappearing after an expiry gap mints a fresh `connection_id` rather than resuming the old one, and that the original stays retained as `expired`.
- [x] Ensure one unavailable/failing provider is isolated and doesn't take down the whole observation session (surface its status instead) — and specifically, ensure a `transient_failure` from `SocketProvider` never gets misread as "all connections disappeared" (see the 4th mandatory test in `docs/TESTING_STRATEGY.md`). Done, covered by `provider_failure_does_not_manufacture_state`.
- [x] The Engine retains exited processes and their terminated connections in `get_processes`/`get_connections` output for the rest of the session, rather than removing them once `process_state == Exited`/`lifecycle_state == Closed` (see the 1st mandatory test in `docs/TESTING_STRATEGY.md`, which asserts on this). Done, covered by `process_termination_closes_its_connections`.

**Concurrency (`tokio`, not asyncio or Swift concurrency)**

- [x] Define the `tokio` task model for providers and `ObservationEngine`. Done, but scoped to what Phase 0.1 actually needs: commands are async and run through `tokio` (via Tauri's async command support), not a spawned continuous polling loop — Phase 0.1's frontend is manual-refresh-only by design ("Frontend" section below), and "Live Monitoring" (a real polling loop) is explicitly Phase 0.2's name and scope. Building a timer-driven loop now with nothing continuous to drive would be scope creep this phase doesn't call for.
- [x] Ensure polling tasks can be cancelled cleanly on shutdown. Vacuously satisfied this phase: there is no persistent background task to cancel (see above) — Tauri's own command lifecycle handles a single in-flight `invoke` call. Revisit once Phase 0.2 adds a real spawned loop.
- [x] Prevent overlapping polling cycles. Done: `ObservationEngine` is wrapped in a `tokio::sync::Mutex` (`commands::EngineState`) — a second concurrent `get_processes`/`get_connections` call queues behind the first's lock guard rather than racing it.
- [x] Ensure event pushes to the frontend don't block the polling loop. Done in the sense Phase 0.1 needs: the `connections-updated` emit in `get_connections` happens after the engine lock is released, so it can't block a subsequent refresh's lock acquisition.

**Tauri commands**

- [x] `get_processes` command — returns the two-level envelope from `docs/OBSERVATION_CONTRACT.md` (`{ status: ObservationStatus, data: Option<Vec<ProcessInfo>> }`): an outer status for the query as a whole, each `ProcessInfo` carrying its own `status`. Done: `src-tauri/src/commands/mod.rs`.
- [x] `get_connections(pid)` command — same envelope shape, `data: Option<Vec<NetworkConnection>>`, each with its own `status`. Done.
- [x] Event stream (Tauri's `emit`/`listen`) for live connection updates, scoped to a selected process. Done on the emit side — `get_connections` emits a `connections-updated` event (pid-scoped payload) alongside its direct response, so Phase 0.2 only has to add the polling loop that calls it repeatedly, not invent the mechanism. Nothing subscribes to it yet: the frontend renders the direct `invoke` response (manual refresh), matching the "Frontend" section below — a `listen()`-driven UI would have nothing meaningful to do without live polling behind it.

**Frontend**

- [x] Build the process list view — searchable table (name, PID, `active_connection_count`, status). Done: `src/components/ProcessList.tsx`.
- [x] Build the process detail view shell with tab structure (Overview / Connections / API Traffic / Timeline — only Connections functional this phase). Done: `src/components/ProcessDetail.tsx`.
- [x] Build the connections view — one row per socket: protocol, local/remote addr+port, state; `bytes_sent`/`bytes_received` render as an explicit "not reported" affordance when absent, never as `0` (field-level-absence rule, `docs/OBSERVATION_CONTRACT.md`). Done: `src/components/ConnectionsView.tsx`.
- [x] Wire up manual refresh (button, not yet live polling). Done — both views fetch once on mount and again only on an explicit Refresh click.
- [x] Define a shared state model for views: `loading` (client-side only, never a backend status) plus the seven `ObservationStatus` states rendered verbatim — `observed` / `unavailable` / `permission_denied` / `unsupported` / `transient_failure` / `stale` / `unmatched` — driven directly by the backend's `ObservationStatus`, not inferred from data shape. There is no `empty` state: an `observed` status with zero rows renders "no connections observed"; any other status renders that status. There is no `error` state: transport failures render as `unavailable`. `stale` uses the Phase 0.1 flat 30s threshold from `docs/OBSERVATION_CONTRACT.md` (no configurable polling interval exists yet this phase). Done: `src/components/StatusBadge.tsx` (the `ViewState` type + rendering) and `src/api/inspector.ts` (catches a rejected `invoke()` — a genuine IPC transport failure — and synthesizes `unavailable`, since there's no separate "error" state per the contract).

**Demo checkpoint:** select a real running process, see its actual open sockets, values match `lsof -p <PID> -i -n -P` run by hand — including a deliberately permission-denied case rendering as "permission denied," not as an empty list (the outer `status` on `get_connections(pid)`'s envelope, per `docs/OBSERVATION_CONTRACT.md`). Automated 2026-09-07 rather than run by hand: `real_socket_matches_lsof` binds a real socket in-process, confirms the Engine (via the real `netstat2` provider) reports it, and independently cross-checks the same fact by shelling out to `lsof -p <pid> -i -n -P` — passes. The permission-denied case is covered by `other_users_pid_is_permission_denied_not_empty` (mocked, since deliberately targeting another user's real PID isn't something a test suite should assume it can do) and was verified for real during the Phase 0 spike (`docs/PERMISSIONS_AND_PLATFORM.md`) against real other-user daemons. `cargo tauri dev` was also run live end-to-end (built, launched, process list + connections UI functioning, no panics) before this checklist was marked done.

## Phase 0.2 — Live Monitoring + DNS/hostname correlation

- [x] Add polling loop with configurable interval (default 2s — see `docs/OBSERVATION_CONTRACT.md`'s staleness formula, which is relative to this value); start/stop controls in UI. Done 2026-09-07: `src-tauri/src/commands/monitoring.rs` — a `tokio::spawn`ed loop, one at a time (starting a new session stops any previous one), stopped via `JoinHandle::abort()` per Phase 0.1's concurrency note. UI: `ProcessDetail`'s "Start/Stop Live Monitoring" button.
- [x] Define monitoring state machine: `idle` / `starting` / `running` / `stopping` / `stopped` / `failed`. Done: `src-tauri/src/models/monitoring.rs`. `starting`/`stopping`/`failed` are defined but not currently reachable — starting/stopping are effectively instantaneous in this implementation (no slow handshake to model), and every provider this phase always succeeds in practice, same "defined, rare-to-unreachable" treatment already given to `closed` in Phase 0.1.
- [x] Track observation timestamp separately from `first_seen`/`last_seen`; surface "last updated" in the UI so a stalled poll never silently implies a live "ESTABLISHED right now" — this is exactly what `ObservationStatus.last_successful_at` is for (now always set once any successful observation has occurred, not only once `stale`). Already correct since Phase 0.1 (`ObservationStatus::from_provider`); no change needed here beyond the threshold formula below.
- [x] Switch `stale`'s threshold from Phase 0.1's flat 30s to the formula in `docs/OBSERVATION_CONTRACT.md`: `now - last_successful_at > 3 × configured poll interval`. Done: `models::status::polling_stale_threshold`, applied once `start_monitoring` calls `Engine::set_poll_interval_ms` — the flat-30s fallback stays in effect until then, matching "no configurable polling interval exists yet" for a session that never starts monitoring.
- [x] Handle selected-process termination: detect the PID no longer exists via `ProcessProvider`, stop monitoring it, mark it "process exited" in the UI, transition its connections to `closed` (the process-exit path in `docs/DATA_MODEL.md`'s `closed`/`expired` rule), and keep its historical observations for the current session rather than clearing them. Done: the polling loop checks the monitored pid's `process_state` each tick and auto-stops (emitting `monitoring-state-changed` with reason `"process exited"`) when it flips to `Exited`; `ProcessDetail` renders a banner. The `closed` transition itself was already Phase 0.1's engine logic — this just drives it from the live loop instead of a one-off manual refresh.
- [x] Emit `TrafficEvent`s for connection-opened (on the `discovered → active` transition) / connection-closed / connection-expired (from the lifecycle transitions already implemented in `ObservationEngine`), using the explicit `connection_id`/`request_id`/`response_id` fields per event type rather than one ambiguous reference field (`docs/DATA_MODEL.md`). Done: `ObservationEngine::record_event`, called from both lifecycle-transition sites; covered by `traffic_events_fire_on_lifecycle_transitions`.
- [x] Build first pass of the timeline view driven by those events. Done: `get_timeline(pid)` command + `src/components/TimelineView.tsx` — full session history on load, live-appended via the `timeline-event` push while monitoring.
- [x] Define `HostnameObservation` (provider-owned) and `ResolvedHostname` (Engine-owned, adds `connection_id`/`status`) per `docs/DATA_MODEL.md` — a provider must never construct `ResolvedHostname` directly, same discipline as every other Engine-owned type. Done: `src-tauri/src/models/hostname.rs`.
- [x] Implement `DNSProvider` (reverse DNS lookup to start; SNI/Host-header sources land in Phase 0.4 once HTTP exists). Done: `ReverseDnsProvider` (`dns-lookup` crate, the system resolver via `getnameinfo`) — resolved through `spawn_blocking` and reported back to the Engine asynchronously (`commands::monitoring::spawn_dns_lookups`) so a slow/hanging lookup can never block the engine lock or the polling loop, per Phase 0.1's "don't block" concurrency principle.
- [x] Surface resolved hostnames in the connections view (e.g. `api.example.com:443` instead of raw IP) using the display rule in `docs/DATA_MODEL.md` (`ResolvedHostname`'s "Display rule when sources disagree") for the case where reverse DNS, SNI, and `Host` disagree. Done for the case that actually exists this phase — one source (`ReverseDns`) per connection, shown when resolved, raw IP as fallback otherwise (`ConnectionsView.tsx`). The multi-source "show both above 0.85" branch has nothing to exercise it until Phase 0.4 adds `Sni`/`HttpHost`; `get_hostnames`'s doc comment flags this so 0.4 doesn't have to rediscover it.

**Demo checkpoint:** open a connection-heavy app (browser, Slack), watch new connections appear/disappear live with resolved hostnames; quit that app and confirm the UI reports "process exited" instead of a stale connection list, with that app's connections now shown `closed`. Verified via the Engine test suite (13 tests, including `traffic_events_fire_on_lifecycle_transitions`, `configured_poll_interval_changes_the_stale_threshold`, `resolved_hostnames_surface_by_connection` — all against the real logic, mocked or real providers as appropriate) plus a live `cargo tauri dev` run (compiled, launched, ran for several seconds with no panics). The literal walkthrough (open Slack, watch it live, quit it) wasn't done by hand in this pass — this agent can't click the native window's buttons — but every mechanism the checkpoint exercises is independently covered by an automated test above, the same standard `real_socket_matches_lsof` set for Phase 0.1's checkpoint.

## Phase 0.3 — Traffic Observation Backend

*Hardest phase. Don't start until 0.1–0.2 are solid and reliable.*

- [ ] Spike: confirm `mitmproxy --mode local:<pid>` works standalone against a process you control, before wiring anything into the app
- [ ] Design the core-to-helper boundary: how the Rust core launches/manages the `mitmdump`-based helper process (`std::process::Command`/`tokio::process`), and the IPC format (local socket + JSON via `serde`) between them
- [ ] Write the mitmproxy addon script: consume `request`/`response` events only — no `intercept()`, no `set()`, no replay hooks, by construction
- [ ] Implement `TrafficProvider` trait + mitmproxy-backed implementation
- [ ] Write the mitmproxy addon so it applies tier-1 (highly-sensitive) redaction before emitting anything over the helper→core IPC socket, per the shared field-name list in `docs/PRIVACY_AND_SECURITY.md`'s "Starter sensitive-field list" — the IPC schema must have no field capable of carrying an unredacted tier-1 value (`docs/PRIVACY_AND_SECURITY.md`'s "two redaction checkpoints")
- [ ] Add `ObservationCapabilities` **per provider** (not per connection) as an Engine-aggregated struct, per `docs/DATA_MODEL.md`, reported by a `get_capabilities()` command — per-connection variability stays in `ObservationStatus`, don't add a capability field to `NetworkConnection`. (Moved here from an earlier Phase 0.2 placement to match `ARCHITECTURE.md` and `DATA_MODEL.md`, both of which already said Phase 0.3.)
- [ ] Define `TrafficProvider` capability reporting (`process`, `http_metadata`, `https_metadata`, `request_body`, `response_body`, ... — the `ObservationCapabilities` fields in `docs/DATA_MODEL.md`, not a separately-invented name set) — distinguish "provider unavailable" from "this traffic type is unsupported"
- [ ] Handle the `unsupported`/`unavailable` path explicitly: pinned certs, QUIC, non-HTTP traffic must degrade to "connected, bytes only" rather than erroring
- [ ] Define `CorrelationEvidence` (pid?, protocol?, local/remote addr+port?, hostname?, timestamp, source) per `docs/DATA_MODEL.md` — the actual input to correlation, since `TrafficProvider` never has the Engine's internal `connection_id`
- [ ] **Spike, independently of the UI: `CorrelationEvidence` → `NetworkConnection` matching.** Determine which identifiers the traffic provider actually exposes (PID? local port? both?) and whether they're sufficient to reliably map a captured flow back to an existing connection
- [ ] Define and implement the `unmatched` path: evidence that can't be confidently mapped produces an `HTTPRequest` with `connection_id = None`, `status.state = Unmatched`, and `evidence` set to the `CorrelationEvidence` it was scored against (`docs/DATA_MODEL.md`) — appears in the UI as such, never silently dropped and never attached to the wrong connection by guessing
- [ ] Principle to enforce in code review, not just docs: never fabricate a `connection_id` when correlation confidence is insufficient
- [ ] Scope capture strictly to the user-selected PID — verify traffic from unrelated processes never leaks into a session

**Demo checkpoint:** select a process, trigger a known HTTP(S) request from it, see the raw flow show up in a debug log correctly attached to its connection (UI integration is next phase).

## Phase 0.4 — HTTP/HTTPS Metadata

- [ ] Define `RawHTTPRequest`/`RawHTTPResponse` (transient, in-memory only — potentially-sensitive fields held raw here) and `HTTPRequest`/`HTTPResponse` (Engine-owned, redacted, the only form that reaches storage/export/frontend — now with explicit `request_id`/`response_id`, `status`, `redacted_fields`, and optional `connection_id`/`evidence` for the `unmatched` case) per `docs/DATA_MODEL.md`
- [ ] Confirm capture-time redaction for the highly-sensitive tier (`Authorization`, API keys, passwords, tokens — the starter list in `docs/PRIVACY_AND_SECURITY.md`) is actually happening in the mitmproxy addon (Phase 0.3), not here — these must never populate even the `Raw*` types on the Rust-core side; there is no reveal path for them, by design
- [ ] Enforce the `body_preview` size limit (8 KiB, `docs/PRIVACY_AND_SECURITY.md`) at truncation time — this is the real enforced limit now, not a Phase 0.6 placeholder
- [ ] Wire `TrafficProvider` output into the Observation Engine, attached to the correct `NetworkConnection` via the correlation logic from Phase 0.3
- [ ] Add SNI and HTTP-`Host`-header as additional `HostnameObservation` sources (alongside reverse DNS from 0.2), using the confidence starter values in `docs/DATA_MODEL.md`
- [ ] Implement the `Redactor` utility for the potentially-sensitive tier: header-based (`Cookie`, `Set-Cookie`) + body/query-field heuristics, starting from the list in `docs/PRIVACY_AND_SECURITY.md`'s "Starter sensitive-field list" (tier 2 portion) — user-extendable, never user-shrinkable — producing `HTTPRequest`/`HTTPResponse` from `RawHTTPRequest`/`RawHTTPResponse` and populating `redacted_fields` with whatever was actually replaced
- [ ] Unit-test the `Redactor`: header redaction, cookie redaction, query-parameter redaction, nested-JSON body-field redaction, case-insensitive matching, configurable custom field names
- [ ] **Verify redaction happens before persistence/export, not only before display** — write a test that captures a request with a fake credential, saves/exports it, and asserts the credential never reaches disk unredacted (see the four mandatory integration tests in `docs/TESTING_STRATEGY.md`)
- [ ] Build the API traffic view — method/URL/status/duration table
- [ ] Build the request/response detail expansion view: redacted by default (using `redacted_fields` to render a distinct "redacted" affordance rather than inferring from content), a per-field "show anyway" action invokes the `reveal_raw(request_id)` Tauri command (added to the capability allowlist alongside the other Phase 0.1 commands) and reveals the paired `Raw*` object for the potentially-sensitive tier only, with highly-sensitive fields never displayable at all

**Demo checkpoint:** trigger a real HTTPS request from a test process, see method/headers/body in the UI with credentials redacted, and confirm a saved session file on disk is also redacted.

## Phase 0.5 — API Explorer

- [ ] Wire the already-defined `Flow` model (Phase 0.1) into the Engine and UI, attaching HTTP observations to it (per `docs/DATA_MODEL.md` — this is the point where it earns its keep, per ADR-004)
- [ ] Group captured requests by host → endpoint in a dedicated API view
- [ ] Compute per-endpoint stats: request count, error rate, average latency
- [ ] Build the "killer feature" tree view: process → domain → requests, collapsible

**Demo checkpoint:** run a chatty process for a few minutes, view its full API surface grouped and summarized.

## Phase 0.6 — Timeline + Session Recording & Playback

- [ ] Finalize the timeline view to interleave connection events and HTTP request/response events chronologically
- [ ] Design session storage format (local file, e.g. JSON/SQLite) capturing a full observation window
- [ ] Implement and document: sessions are stored **redacted-only, unconditionally** — no raw/unredacted storage mode, opt-in or otherwise (`docs/PRIVACY_AND_SECURITY.md`)
- [ ] Revisit the body-preview size limit (8 KiB, defined in Phase 0.4 per `docs/PRIVACY_AND_SECURITY.md`) as part of the full memory-budget/eviction-policy design — this phase covers the header/session memory limits and eviction policy the number itself already exists for, not a re-decision of the number
- [ ] Implement the `RawHTTPRequest`/`RawHTTPResponse` lifetime rule from `docs/DATA_MODEL.md`: destroy raw transient objects when a session ends (not just dereference-and-hope), cap the number of retained raw objects per session, and define the eviction policy once that cap is hit
- [ ] Ensure captured request/response contents are never written into general application logs, and audit whatever logging crate (`log`/`tracing`) and Tauri's own logging plugin are configured with so request URLs (which can carry query-string secrets) aren't logged independently of application code, bypassing the `Redactor` entirely
- [ ] Document where session files are stored on disk
- [ ] Implement "start session" / "stop & save session"
- [ ] Implement session list + reopen-for-viewing (explicitly not "resend" — no code path should be able to turn a saved request back into an outbound one)

**Demo checkpoint:** record a session, quit the app, relaunch, reopen and replay the timeline visually; inspect the saved session file directly and confirm no unredacted credential is present.

## Phase 0.7 — Filters + Search + Analytics

- [ ] Implement query-style filtering: `host:`, `status:`, `method:`, `port:`
- [ ] Add filter bar to the API traffic view and connections view
- [ ] Add basic analytics: totals, error counts, latency distribution (per process and per session)

## Phase 1.0 — Local-Run Polish + Packaged Distribution

*Under the FastAPI-era stack, packaged distribution was explicitly deferred past 1.0 (see the old Phase 2 below) because it wasn't achievable without a separate rewrite. Under Tauri (`DECISIONS.md` ADR-013), a signed native build is close to free — `tauri build` plus signing config, not a separate project — so it belongs in 1.0's actual definition of done, not a "someday" phase.*

- [ ] Build the actual "Observation Capabilities" panel UI, rendering the `ObservationCapabilities` fields from `docs/DATA_MODEL.md` directly (data plumbing and capability reporting both since Phase 0.3)
- [ ] Dark mode pass
- [ ] Export (session as JSON, or a single flow as text) — export only, never re-send; export must go through the same redacted-by-default path as session storage
- [ ] Performance pass: polling overhead, large-session memory use, confirm resource limits from 0.6 actually hold under sustained high-traffic load
- [ ] Code signing and notarization: Apple Developer certificate, `tauri.conf.json` signing identity, `notarytool` submission — set this up early enough in the phase to catch certificate/entitlement issues before they're a release blocker
- [ ] Write a reliable build/run guide: `cargo tauri build` for the distributable signed `.app`, plus a `cargo tauri dev` note for running from source during development
- [ ] Write end-user README: what this tool does, what it deliberately does not do, and the "things you own or have permission to inspect" scope note from the original learning path
- [ ] Run a full end-to-end regression pass before calling this 1.0: process discovery → socket observation → lifecycle tracking → DNS correlation → HTTP observation → redaction → session storage → session reopening. A checklist walkthrough is enough at this project's scale — this doesn't need CI infrastructure, just a deliberate pass through the whole chain instead of assuming the individual phase demo checkpoints still compose correctly together

## Phase 2 — Network Extension Upgrade (future, optional — not required to consider this project done)

Only pursue this if mitmproxy's local-mode ceiling (certificate pinning, no packet-level capture) becomes an actual blocker for something you need to see. Not part of 1.0's definition of done — 1.0 ships with the mitmproxy-based `TrafficProvider` as its traffic-capture mechanism, packaged and signed.

- [ ] Design and build a Swift/ObjC system-extension target implementing `NEPacketTunnelProvider`/`NEFilterDataProvider`, embedded inside this app's existing signed `.app` bundle (see `docs/DECISIONS.md` ADR-013 and `docs/PERMISSIONS_AND_PLATFORM.md` — this is still real, separate native work even though the host app is already native). **The extension is observe-only by construction:** a `NEFilterDataProvider` implementation must return an allow verdict on every flow unconditionally, with no configuration path making that conditional — `NEFilterDataProvider` is a filtering API by nature, and any deviation from unconditional-allow is a violation of `docs/process-network-inspector-report.md` §2 that requires reopening that section first, not a Phase 2 implementation detail.
- [ ] File the Network Extension entitlement request early and treat Apple's review lead time as its own milestone
- [ ] Design the XPC or equivalent IPC boundary between the main Rust core and the Swift extension, following the same `TrafficProvider` interface contract so the rest of the Engine doesn't need to change

---

## Cross-cutting (ongoing, not a single phase)

- [ ] Build a small deterministic `NetworkTestTarget` script early (useful starting in Phase 0.1, essential by 0.3) that generates known traffic on demand: a plain TCP connection, a short-lived connection, a long-lived connection, several simultaneous connections, a couple of HTTP(S) requests once Phase 0.3 exists, and requests carrying intentionally fake sensitive-looking fields for redaction testing. This is a standalone traffic-generating script, not part of the app itself — Python is a fine, simple choice for it regardless of the app's own stack. Same principle the source learning path opened with — verify the tool against traffic you already understand before pointing it at anything else.
- [ ] Unit tests per provider (`cargo test`, mock the system-call boundary so tests don't depend on real running processes)
- [ ] Add integration tests for the observation pipeline using `NetworkTestTarget`: generate known connections/HTTP requests and verify they come out the other end with correct process attribution, connection identity, lifecycle events, hostname correlation, HTTP correlation, and redaction — unit tests per provider don't catch a correlation bug in `ObservationEngine`, only a test that exercises the full chain does. This includes the four mandatory tests in `docs/TESTING_STRATEGY.md`: process termination, polling-gap → `expired`, correlation ambiguity → `unmatched`, and provider failure must not manufacture expiry.
- [ ] Keep `docs/PERMISSIONS_AND_PLATFORM.md`'s VERIFIED/ASSUMED/DECISION tags current as permission reality gets discovered
- [ ] Re-check the "explicitly out of scope" list (`docs/process-network-inspector-report.md` Section 2) at the start of every phase — no feature in this roadmap should ever grow into edit/replay/inject
- [x] **Code-review gap (1/5):** `ProcessObservation`/`ProcessInfo.executable_path` silently rendered as `""` when both `sysinfo::Process::exe()` and the `proc_pidpath` FFI fallback failed — a real case (`pid 0`/`kernel_task`, which `sysinfo` does enumerate on macOS), and a direct violation of this file's own line 5 / `OBSERVATION_CONTRACT.md`'s no-inference rule: an empty string is indistinguishable from a real, observed empty path. Done 2026-09-07: changed `executable_path` to `Option<String>` in both types (`src-tauri/src/models/process.rs`), propagated `None` instead of `.unwrap_or_default()` in the provider (`src-tauri/src/providers/process.rs`), updated the Engine's `TrackedProcess`/`ProcessInfo` construction (`src-tauri/src/engine/mod.rs`), the frontend type (`src/api/types.ts`), and added a "Path" column to `ProcessList.tsx` rendering an explicit "path unknown" affordance when `null` — same pattern `ConnectionsView.tsx` already used for `bytes_sent`/`bytes_received`. Covered by `unknown_executable_path_is_none_not_empty_string`. `cargo test` (14/14), `cargo clippy`, `tsc --noEmit` all clean.
- [x] **Code-review gap (2/5):** `ProcessObservation.name` was `sysinfo::Process::name()` directly, which reads the kernel's `pbi_comm` field — truncated to `MAXCOMLEN` (16 bytes) on macOS, so e.g. "Google Chrome Helper (Renderer)" showed as "Google Chrome H". A real, silent data-quality bug, not a display nit. Done 2026-09-07: `providers/process.rs`'s `derive_name` now takes `name` from `executable_path`'s basename whenever the path is known (no truncation limit there), falling back to the raw `sysinfo` name only when the path itself is `None` — depends on gap 1/5's fix landing first, since it needs `executable_path` to genuinely distinguish "known" from "unknown." Documented as a known macOS kernel limitation in `docs/DATA_MODEL.md` near `ProcessObservation` (which also corrected that table's `executable_path` row — still `String`/required there since gap 1/5's fix, a code-only change, hadn't updated it). Covered by `prefers_path_basename_over_truncated_kernel_name`/`falls_back_to_kernel_name_when_path_unknown`. `cargo test` (16/16), `cargo clippy` clean.
- [x] **Code-review gap (3/5):** `ProcessObservation.cpu_percent` was structurally broken — always ~0%. Root cause: `SysinfoProcessProvider::snapshot()` created a brand-new `sysinfo::System::new_all()` and refreshed it once, every call; `sysinfo` computes `cpu_usage()` as a delta against that *same* `System`'s previous refresh, so a fresh instance has no prior sample and reports ~0% on every single call, unconditionally — not an occasional glitch, guaranteed every time. Currently dormant (`cpu_percent` isn't rendered anywhere in the frontend yet) but already wired into `src/api/types.ts`, so it would have silently shipped wrong data the first time a later phase displayed it. Done 2026-09-07: `SysinfoProcessProvider` now holds a `Mutex<System>` (interior mutability, since `ProcessProvider::snapshot` takes `&self` not `&mut self` — `docs/ARCHITECTURE.md`) and refreshes the *same* instance every call instead of rebuilding it. Covered by `cpu_percent_becomes_measurable_after_second_refresh_of_same_instance`, which burns real CPU between two refreshes of one provider instance and asserts the delta is actually measurable — the old code would have failed this deterministically (every process's first-ever refresh is ~0% by `sysinfo`'s own design). `cargo test` (17/17), `cargo clippy` clean.

## Definition of done for the whole project

Select a running process and obtain Levels 1–3 (process, network, protocol) wherever those observations are technically and legitimately available, with the UI explicitly distinguishing **observed**, **denied**, **unsupported**, and **stale** information rather than presenting any of them as empty or current. Level 4 (HTTP/application payload) is available only where technically and legitimately observable, degrading to the same explicit categories rather than silence. Highly-sensitive data is never captured in raw form at all; potentially-sensitive data is redacted by default and irreversibly before it ever reaches storage or export. Nothing in the app is capable of modifying, replaying, or injecting traffic.
